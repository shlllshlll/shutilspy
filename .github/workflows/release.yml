# This workflow will build a python wheel and public to github release page
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-python

name: Python package

on:
  push:
    tags:
      - "v*.*.*"
  workflow_dispatch:
    inputs:
      tag:
        description: The tag to publish
        required: true

jobs:
  release:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash -l {0}
    environment:
      name: pypi
      url: https://pypi.org/p/shutilspy
    permissions:
      id-token: write
      contents: write
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.14"]
    steps:
      - name: Set Variable
        env:
          INPUT_TAG: ${{ inputs.tag }}
          PUSH_TAG: ${{ github.ref }}
        run: |
          if [ -z "${INPUT_TAG}" ]; then
            TAG=${PUSH_TAG}
          else
            TAG=${INPUT_TAG}
          fi
          if [[ "${TAG}" == refs/tags/* ]]; then
            VERSION="${TAG#refs/tags/}"
          else
            VERSION="${TAG}"
          fi
          echo "TAG=${TAG}" >> $GITHUB_ENV
          echo "VERSION=${VERSION}" >> $GITHUB_ENV
      - uses: actions/checkout@v4
        with:
          ref: ${{ env.TAG }}
      # - name: Set up Python ${{ matrix.python-version }}
      #   uses: actions/setup-python@v5
      #   with:
      #     python-version: ${{ matrix.python-version }}
      - uses: conda-incubator/setup-miniconda@v3
        with:
          auto-update-conda: true
          python-version: ${{ matrix.python-version }}
          channels: conda-forge
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install flake8 pytest
          python -m pip install build setuptools
          conda install -n base conda-build anaconda-client
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
      - name: Build wheel
        run: |
          python -m build -nw
      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          skip-existing: true
      - name: Build conda package shutilspy
        run: |
          mkdir -p conda-dist
          OUT_FILE=$(conda build -b --output -c conda-forge recipe)
          conda build --no-test --no-anaconda-upload -c conda-forge recipe
          cp $OUT_FILE conda-dist/
      - name: Build conda package shutilspy-visualizer
        run: |
          OUT_FILE=$(conda build -b --output -c conda-forge recipe-visualizer)
          conda build --no-test --no-anaconda-upload -c conda-forge recipe-visualizer
          cp $OUT_FILE conda-dist/
      - name: Upload to Anaconda.org
        env:
          ANACONDA_TOKEN: ${{ secrets.ANACONDA_TOKEN }}
        run: |
          anaconda --token "$ANACONDA_TOKEN" upload --user shlllhub --label main conda-dist/*
      - name: Create Release
        id: create_release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.TAG }}
          name: Release ${{ env.VERSION }}
          files: |
            dist/*
            conda-dist/*
          draft: false
          prerelease: false
